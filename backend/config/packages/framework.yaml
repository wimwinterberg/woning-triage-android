# see https://symfony.com/doc/current/reference/configuration/framework.html
framework:
    secret: '%env(APP_SECRET)%'

    # Bearer tokens only. A PHP session cookie after the JSON body is sent
    # appends a second payload ("headers already sent") that Android cannot parse.
    session: false

    # ngrok, Docker and App Platform sit in front of PHP. Trust forwarded proto/host.
    trusted_proxies: '127.0.0.1,REMOTE_ADDR,PRIVATE_SUBNETS'
    trusted_headers: ['x-forwarded-for', 'x-forwarded-host', 'x-forwarded-proto', 'x-forwarded-port', 'x-forwarded-prefix']

    #esi: true
    #fragments: true

when@test:
    framework:
        test: true
        session:
            storage_factory_id: session.storage.factory.mock_file
